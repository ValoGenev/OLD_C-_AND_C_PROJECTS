#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    POINTER =  object that stores the memory address of
    another value located in computer memory.

    int example=5;
    int *pointer;
    pointer=&example;



    CONST STRING = const is the prefix of a constant variable.
     One that doesnt change at runtime.

    const char *dani = "autist";
    printf(dani);




}
